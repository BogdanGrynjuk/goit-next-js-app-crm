import React from 'react';

interface PageProps {}
const page = ({}: PageProps) => {
  return (
    <main>
      <h1 className="text-3xl">Dashboard Page</h1>
    </main>
  );
};

export default page;
